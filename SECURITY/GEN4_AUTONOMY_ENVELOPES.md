# Autonomy Envelopes

Each operation has an explicit envelope: environment, artifact, allowed capabilities, resource/time/network quotas, budget ceiling, approval reference, evidence hash, expiry, and rollback target. Envelope violations pause and quarantine; no fallback expands scope. Human emergency stop supersedes every envelope.
