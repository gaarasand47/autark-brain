# Observability

Emit immutable, append-only audit events for every intent, approval, lease, deployment, health sample, pause, rollback, maintenance action, and shutdown. Metrics and traces include artifact/version, correlation ID, timestamps, resource use, error rates, and policy outcome. Clock synchronization, alert routing, retention, export, and tamper-evident checksums are required.
