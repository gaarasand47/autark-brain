# Builder–Operator Interface

Builder supplies an accepted artifact manifest and checksum. OperatorSystem accepts only immutable, tested artifacts and cannot ask Builder to mutate production or bypass sandbox and policy gates.
