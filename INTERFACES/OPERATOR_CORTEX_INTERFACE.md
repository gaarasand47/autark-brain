# Operator–Cortex Interface

Cortex may submit an operator plan and rationale. It cannot mutate lifecycle state, approve transitions, select credentials, override policy, or trigger deployment. OperatorSystem validates plan schema, provenance, budget, and approval state before producing an intent.
