# Policy–Operator Interface

Policy evaluates artifact, environment, capability, network, data, and budget constraints. OperatorSystem receives immutable decisions and cannot override or reinterpret denied constraints.
